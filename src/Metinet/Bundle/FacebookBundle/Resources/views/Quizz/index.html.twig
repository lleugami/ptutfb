{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Quizz list</h1> <a href="{{ path('admin') }}">Tableau De Bord</a>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Picture</th>
                <th>Nombre de participant</th>
                <th>Taux de réussite moyen</th>
                <th>Shortdesc</th>
                <th>Longdesc</th>
                <th>Winpoints</th>
                <th>Averagetime</th>
                <th>Txtwin1</th>
                <th>Txtwin3</th>
                <th>Txtwin2</th>
                <th>Txtwin4</th>
                <th>Sharewalltitle</th>
                <th>Sharewalldesc</th>
                <th>Ispromoted</th>
                <th>Createdat</th>
                <th>State</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('quizz_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td><img src="{{ entity.picture }}" alt="{{ entity.title }}" /></td>
                <td>{{ nbUserByQuizz[entity.id] }}</td>
                <td>{{ tauxReussite[entity.id] }} %</td>
                <td>{{ entity.shortDesc }}</td>
                <td>{{ entity.longDesc }}</td>
                <td>{{ entity.winPoints }}</td>
                <td>{{ entity.averageTime }}</td>
                <td>{{ entity.txtWin1 }}</td>
                <td>{{ entity.txtWin3 }}</td>
                <td>{{ entity.txtWin2 }}</td>
                <td>{{ entity.txtWin4 }}</td>
                <td>{{ entity.shareWallTitle }}</td>
                <td>{{ entity.shareWallDesc }}</td>
                <td>{{ entity.isPromoted }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.state }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('quizz_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('quizz_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('quizz_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>

    {% endblock %}
